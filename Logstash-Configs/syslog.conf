input {
  udp {
    id => "neta_cisco_catalyst_10024_udp"
    port => 10024
    add_field => {"[log][sender][name]" => "cisco_catalyst"}
  }
  tcp {
    id => "neta_cisco_catalyst_10024_tcp"
    port => 10024
    add_field => {"[log][sender][name]" => "cisco_catalyst"}
  }
}

output {
  pipeline {
    send_to => prod_k8s_out
  }
}
